name: basic-lint
on: [push, pull_request]
jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify scripts exist
        run: |
          test -f scripts/bootstrap/01_init_hdfs.sh
          test -f scripts/validation/check_stack.sh
